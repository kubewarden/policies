#!/usr/bin/env bats

# Generated by hack/generate-rego-e2e.py. Do not edit by hand.

@test "accept: images enabled required" {
  run kwctl run -e gatekeeper annotated-policy.wasm \
      -r test_data/test_images_enabled_required.json \
      -s test_data/test_images_enabled_required-settings.json

  # this prints the output when one of the checks below fails
  echo "output = ${output}"

  [ "$status" -eq 0 ]
  [ $(expr "$output" : '.*allowed.*true') -ne 0 ]
}

@test "reject: images enabled violation" {
  run kwctl run -e gatekeeper annotated-policy.wasm \
      -r test_data/test_images_enabled_violation.json \
      -s test_data/test_images_enabled_violation-settings.json

  # this prints the output when one of the checks below fails
  echo "output = ${output}"

  [ "$status" -eq 0 ]
  [ $(expr "$output" : '.*allowed.*false') -ne 0 ]
  [[ "$output" == *"The 'spec.images' field in a Kustomization object must have values based on the policy input"* ]]
}
