# Kubewarden Artifacthub Package config
#
# Use this config to submit the policy to https://artifacthub.io.
#
# This config can be saved to its default location with:
#   kwctl scaffold artifacthub > artifacthub-pkg.yml 
version: 0.1.4
name: labels
displayName: Labels
createdAt: 2025-12-12T15:38:45.329316464Z
description: A policy that validates labels of Kubernetes' resources
license: Apache-2.0
homeURL: https://github.com/kubewarden/labels-policy
containersImages:
- name: policy
  image: ghcr.io/kubewarden/policies/labels:v0.1.4
keywords:
- labels
links:
- name: policy
  url: https://github.com/kubewarden/labels-policy/releases/download/v0.1.4/policy.wasm
- name: source
  url: https://github.com/kubewarden/labels-policy
install: |
  The policy can be obtained using [`kwctl`](https://github.com/kubewarden/kwctl):
  ```console
  kwctl pull ghcr.io/kubewarden/policies/labels:v0.1.4
  ```
  Then, generate the policy manifest and tune it to your liking. For example:
  ```console
  kwctl scaffold manifest -t ClusterAdmissionPolicy registry://ghcr.io/kubewarden/policies/labels:v0.1.4
  ```
maintainers:
- name: Kubewarden developers
  email: cncf-kubewarden-maintainers@lists.cncf.io
provider:
  name: kubewarden
recommendations:
- url: https://artifacthub.io/packages/helm/kubewarden/kubewarden-controller
annotations:
  kubewarden/mutation: 'false'
  kubewarden/questions-ui: |
    questions:
      - default: "containsAnyOf"
        description: "Labels validation criteria"
        group: Settings
        required: true
        label: "Criteria"
        variable: "criteria"
        type: enum
        options:
          - "containsAnyOf"
          - "doesNotContainAnyOf"
          - "containsAllOf"
          - "doesNotContainAllOf"
          - "containsOtherThan"
          - "doesNotContainOtherThan"
      - default: []
        label: "Labels"
        description: "Label names to be validated with the resources definition"
        group: Settings
        variable: values
        required: true
        type: array[
  kubewarden/resources: '*'
  kubewarden/rules: |
    - apiGroups:
      - ''
      apiVersions:
      - v1
      resources:
      - pods
      operations:
      - CREATE
      - UPDATE
    - apiGroups:
      - ''
      apiVersions:
      - v1
      resources:
      - replicationcontrollers
      operations:
      - CREATE
      - UPDATE
    - apiGroups:
      - apps
      apiVersions:
      - v1
      resources:
      - deployments
      - replicasets
      - statefulsets
      - daemonsets
      operations:
      - CREATE
      - UPDATE
    - apiGroups:
      - batch
      apiVersions:
      - v1
      resources:
      - jobs
      - cronjobs
      operations:
      - CREATE
      - UPDATE
